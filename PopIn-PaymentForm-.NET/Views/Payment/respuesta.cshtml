@{
    ViewData["Title"] = "Respuesta IPN";
    Layout = "~/Views/Shared/_Layout.cshtml"; 
}



<div class="container">
    <h2>Respuesta IPN</h2>
    <pre><code class="json">@Html.Raw(ViewBag.valor)</code></pre>
</div>

@section Extracss {
                <style>
                    .container {
                        margin: 20px auto;
                        padding: 15px;
                        background-color: #fff;
                        border-radius: 8px;
                        width: 90%;
                        max-width: 1000px;
                        box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
                        overflow: auto; 
                    }

                    h2 {
                        color: #333;
                        text-align: center;
                        margin-bottom: 20px;
                    }

                    pre {
                        background-color: #f5f5f5;
                        padding: 15px;
                        border-radius: 8px;
                        overflow: auto;
                        white-space: pre-wrap;
                        color: #333;
                        max-height: 80vh; 
                    }

                    .json {
                        color: #000;
                    }
                </style>
}
